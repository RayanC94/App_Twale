/**
 * Types Supabase — placeholder permissif tant que la génération automatique
 * n'est pas en place (nécessite la CLI Supabase).
 *
 * Régénérer après chaque migration :
 *   npx supabase gen types typescript \
 *     --project-id gnwqycedllwyincbllpo > src/lib/supabase/types.ts
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
